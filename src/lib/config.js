// The repo this site is built from. Live scores are read from it directly
// through the GitHub API (see liveScores.js), so this must match the repo.
export const REPO = "mdw22/weeklyfantasyanalyzer";
